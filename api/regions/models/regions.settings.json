{
  "kind": "collectionType",
  "collectionName": "regions",
  "info": {
    "name": "regions"
  },
  "options": {
    "increments": true,
    "timestamps": true
  },
  "attributes": {
    "name": {
      "type": "string"
    },
    "users": {
      "via": "region",
      "plugin": "users-permissions",
      "collection": "user"
    }
  }
}
